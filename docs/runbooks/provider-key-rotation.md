# 供应商密钥轮换

1. 在密钥管理系统创建新密钥，不写入仓库、镜像、工单或聊天记录。
2. 在预发布 Worker 注入新版本 Secret，重启后以不含正文的假请求验证。
3. 滚动更新生产 Worker；观察 401/403、429、失败率和 TTFT 至少 15 分钟。
4. 确认所有实例使用新 Secret 后撤销旧密钥。
5. 扫描结构化日志确认没有密钥片段，并记录轮换时间、操作者和 Secret 版本号。

若失败，恢复上一 Secret 版本并滚动重启；不得把密钥打印到命令行或日志中诊断。
